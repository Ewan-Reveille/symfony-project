<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ article.title }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { font-size: 28px; margin-bottom: 10px; }
        p { font-size: 16px; }
        .meta { font-size: 12px; color: gray; margin-bottom: 20px; }
        .artist { margin-top: 20px; }
    </style>
</head>
<body>
    <h1>{{ article.title }}</h1>
    <div class="meta">
        {{ article.createdAt|date('d F Y') }} — Auteur : {{ article.user.email }}
        {% if artist %}
            | Artiste : {{ artist.name }}
        {% endif %}
    </div>

    <p>{{ article.content }}</p>

    {% if artist %}
        <div class="artist">
            <h2>À propos de l'artiste : {{ artist.name }}</h2>
            {% if artist.image %}
                <img src="{{ artist.image }}" alt="Image de l'artiste" style="max-width: 200px;">
            {% endif %}
            <p>Genre : {{ artist.description }}</p>
            <p>Followers : {{ artist.followers|number_format }}</p>
        </div>
    {% endif %}
</body>
</html>
